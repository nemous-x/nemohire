# {{id}} — {{Role}} @ {{Company}}

One file per job: `./.claude/nemohire/jobs/details/<seq>-<id>.md` — see `templates/tracker/jobs-ledger-schema.md`. This file doesn't exist while a job is `new` or `queued` — `apply-agent` composes every section below in memory as it works the job, live off the actual page, and writes the whole file once, in a single write, at the very end.

## Posting
<!-- Verbatim: description, requirements, posting URL, application URL, as apply-agent found them on the actual page. Copied as-is, never paraphrased. Brief or absent (with a note) if the page never loaded. -->

## Company context
<!-- Short highlight of what the company/product does — a few sentences, capped ~40 lines. Written by apply-agent while browsing. -->

## Cover letter
<!-- Written by apply-agent, in the user's own first-person voice, grounded in identity/. -->

## Answers
<!-- Every real custom question the form asked and the exact answer submitted. Omit this whole section if the form had none. -->

Q:
A:

## Application record
- Files uploaded:
- Submitted (ISO-8601):
- Outcome:
- Note:
